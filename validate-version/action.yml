name: 'Validate Release Version'
description: 'Validate that the new version is valid, and increasing.'
inputs:
  version:
    description: 'The new version.'
    required: true
runs:
  using: 'node12'
  main: 'dist/index.js'
